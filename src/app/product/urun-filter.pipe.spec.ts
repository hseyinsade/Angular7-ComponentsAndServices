import { UrunFilterPipe } from './urun-filter.pipe';

describe('UrunFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UrunFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
